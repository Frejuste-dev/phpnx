# Dépendances Python pour PHPNX
# Le Phoenix s'élève avec ces outils essentiels

# Gestion des processus système
psutil>=5.9.0

# Interface graphique native
# tkinter est inclus avec Python par défaut sur la plupart des systèmes

# Utilitaires pour les requêtes HTTP (pour futures fonctionnalités)
requests>=2.28.0

# Gestion des couleurs dans la console
colorama>=0.4.6

# WebSocket pour interface temps réel
websockets>=10.4

# SSL et cryptographie
cryptography>=3.4.8
pyOpenSSL>=22.0.0

# Outils de développement
watchdog>=2.1.9  # Pour surveiller les changements de fichiers

# Interface graphique avancée (optionnel)
# PyQt5>=5.15.0  # Décommentez pour PyQt5
# PyQt6>=6.0.0   # Décommentez pour PyQt6

# Docker SDK (pour intégration Docker)
docker>=6.0.0

# Outils de compression et archivage
zipfile36>=0.1.3  # Backport pour Python < 3.6

# Monitoring et métriques
prometheus-client>=0.14.0

# Configuration et parsing
toml>=0.10.2
pyyaml>=6.0